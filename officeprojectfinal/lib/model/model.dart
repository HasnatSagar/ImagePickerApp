class UserInfo {
  int id;
  String fullName, emailid, password;

  UserInfo(
      {required this.id,
      required this.fullName,
      required this.emailid,
      required this.password});
}
